describe("GET /tips", () => {
  it("responds with 404 NotFoundError if user does not exist");

  it("responds with 200 { tips }");

  it("accepts recipientId as query param to filter tips by recipient");

  it("accepts senderId as query param to filter tips by sender");
});
